{
  "scripts": {
    "dev": "nodemon",
    "build": "npm run clean-build && tsc",
    "start": "node dist/index.js",
    "clean-build": "rimraf dist"
  },
  "devDependencies": {
    "@types/config": "^3.3.0",
    "@types/node": "^18.8.0",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "config": "^3.3.8",
    "dotenv": "^16.0.3",
    "mongoose": "^6.6.3"
  }
}
